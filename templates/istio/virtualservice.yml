apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: "{{ service_name }}"
spec:
  gateways:
    - "{{ gateway_name }}"
    - mesh
  hosts:
    - "{{ gateway_host }}"
  http:
    - route:
      - destination:
          host: "{{ gateway_host }}"
